import {
    makeStyles,
} from '@material-ui/core';

export const useStyles = makeStyles(() => ({
    root: {
        color: '#000000',
        position: 'relative',
        top: 100
    },
    toggle: {
        padding: 10,

    }
}));